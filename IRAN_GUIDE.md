# راهنمای استفاده در ایران

## ⚠️ مشکل: سرویس‌های خارجی فیلتر یا کند هستند

متأسفانه اکثر سرویس‌های عمومی مثل `httpbin.org` از ایران فیلتر یا بسیار کند هستند.

---

## ✅ بهترین راه‌حل: استفاده از سرور محلی (Local Server)

این روش **سریع‌ترین** و **بدون محدودیت** است:

### مرحله 1: راه‌اندازی سرور محلی

```bash
# ترمینال اول - سرور رو اجرا کن
python dummy_server.py -p 8080
```

### مرحله 2: شروع آپلود

```bash
# ترمینال دوم - آپلود رو شروع کن
python fake_upload.py -g 400 -p 8080 -t 8
```

این روش:
- ✅ **خیلی سریع** (تا 1GB/s یا بیشتر)
- ✅ بدون فیلتر
- ✅ بدون محدودیت حجم
- ✅ مصرف اینترنت ندارد (فقط localhost)
- ✅ مناسب برای بالا بردن نمودار آپلود

---

## 🌐 راه‌حل دوم: استفاده از سرور خودتان

اگر یک سرور خارجی (VPS) دارید:

### روی سرور خارجی:

```bash
# روی سرور VPS خودتان
python dummy_server.py -H 0.0.0.0 -p 8080
```

### روی سیستم محلی:

```bash
# YOUR_SERVER_IP را با آی‌پی سرورتان عوض کنید
python fake_upload.py -g 400 -H YOUR_SERVER_IP -p 8080 -t 8
```

---

## 🚀 راه‌حل سوم: استفاده از تونل (مثلاً با ngrok)

اگر سرور ندارید، می‌توانید با ngrok یک تونل عمومی بسازید:

### مرحله 1: نصب ngrok

```bash
# دانلود از https://ngrok.com/download
# یا با brew در macOS:
brew install ngrok
```

### مرحله 2: راه‌اندازی سرور محلی

```bash
python dummy_server.py -p 8080
```

### مرحله 3: ایجاد تونل

```bash
# ترمینال جدید
ngrok http 8080
```

ngrok یک URL عمومی می‌دهد مثل: `https://abc123.ngrok.io`

### مرحله 4: آپلود از هر جا

```bash
python fake_upload.py -g 400 -H abc123.ngrok.io -p 443 -t 8
```

---

## 📊 مقایسه روش‌ها

| روش | سرعت | فیلترینگ | نصب | مناسب برای |
|-----|------|----------|-----|-----------|
| **سرور محلی** | ⭐⭐⭐⭐⭐ بسیار سریع | ✅ هیچ مشکلی | ✅ آسان | تست و توسعه |
| **VPS خودتان** | ⭐⭐⭐⭐ سریع | ✅ اگر VPS فیلتر نباشد | 🔧 نیاز به VPS | استفاده واقعی |
| **ngrok/تونل** | ⭐⭐⭐ متوسط | ⚠️ ممکن است کند شود | 🔧 نیاز به نصب | اشتراک‌گذاری موقت |
| **سرویس‌های عمومی** | ⭐ خیلی کند | ❌ اکثراً فیلتر | ✅ آسان | از ایران مناسب نیست |

---

## 💡 توصیه نهایی

**برای استفاده در ایران، حتماً از روش سرور محلی استفاده کنید:**

```bash
# ترمینال 1
python dummy_server.py -p 8080

# ترمینال 2
python fake_upload.py -g 400 -p 8080 -t 8
```

این روش:
- هیچ اینترنتی مصرف نمی‌کند
- خیلی سریع است (محدودیت فقط CPU و RAM)
- هیچ فیلتری ندارد
- برای بالا بردن نمودار آپلود مناسب است

---

## 🎯 مثال‌های عملی

### آپلود بسیار سریع (محلی):

```bash
# سرور
python dummy_server.py -p 8080

# آپلود 1TB با 16 thread
python fake_upload.py -g 1000 -p 8080 -t 16 -s 20
```

### آپلود متوسط (محلی):

```bash
# سرور
python dummy_server.py -p 8080

# آپلود 400GB با 8 thread
python fake_upload.py -g 400 -p 8080 -t 8
```

### آپلود کند (محلی):

```bash
# سرور
python dummy_server.py -p 8080

# آپلود 100GB با 4 thread
python fake_upload.py -g 100 -p 8080 -t 4
```

---

## ❓ سوالات متداول

### 1. چرا باید از localhost استفاده کنم؟
- خیلی سریعتر است
- هیچ فیلتری ندارد
- اینترنت مصرف نمی‌کند

### 2. آیا واقعاً آپلود انجام می‌شود؟
- بله! داده‌های تصادفی تولید و ارسال می‌شوند
- فقط به جای اینترنت، روی localhost ارسال می‌شود
- CPU و RAM مصرف می‌شود

### 3. چطور نمودار آپلود رو ببینم؟
- مرورگر را باز کن
- برو به: `http://localhost:8080`
- آمار و نمودار زنده را می‌بینی

### 4. چند thread مناسب است؟
- سیستم ضعیف: 4 thread
- سیستم متوسط: 8 thread
- سیستم قوی: 16 thread یا بیشتر

### 5. آیا می‌تونم همزمان چند instance اجرا کنم؟
- بله! هر کدام روی پورت جدا:

```bash
# سرور 1
python dummy_server.py -p 8080

# سرور 2
python dummy_server.py -p 8081

# آپلود به هر دو
python fake_upload.py -g 200 -p 8080 -t 4 &
python fake_upload.py -g 200 -p 8081 -t 4 &
```

---

## 🔧 عیب‌یابی

### خطا: Connection Refused
```bash
# مطمئن شوید سرور در حال اجراست
ps aux | grep dummy_server.py

# اگر نبود، دوباره اجرا کنید
python dummy_server.py -p 8080
```

### خطا: Port Already in Use
```bash
# پورت دیگری انتخاب کنید
python dummy_server.py -p 8081
python fake_upload.py -g 400 -p 8081 -t 8
```

### سرعت خیلی کمه
```bash
# تعداد thread ها را افزایش دهید
python fake_upload.py -g 400 -p 8080 -t 16

# یا سایز chunk را کم کنید
python fake_upload.py -g 400 -p 8080 -t 16 -s 5
```
